<div class="col-md-6" ng-controller="formGeneratorController">
    <h3>Preview</h3>
    <div class="row">
        <div ng-repeat="item in items">
            <div class="panel">
                <div class="panel-body form-group">
                    <!-- $scope.newInput={
                        id: $scope.lastInputId++,
                        type: $scope.possibleInputTypes[0],
                        name: "" ,
                        description: "" ,
                        isRequired: true,
                        textMinLength: 0,
                        textMaxLength: -1,
                        clicableOptions: []
                        };
                        -->
                        <div>
                            <div class="form-group">
                                <label for="itemid_{{ item.id }}">{{item.name + item.isRequired ? "*" : ""}}:</label>
                                <input type="text" class="form-control" id="itemid_{{ item.id }}" required="{{item.isRequired}}" />
                            </div>
                        </div>
                </div>
            </div>
        </div>
    </div>
</div>
